# Puerto expuesto en el host para la API (usado por docker-compose)
API_PORT=8000

# Carpeta donde la API busca model.pkl y metadata.json
ARTIFACTS_DIR=artifacts

# Registro de contenedores donde se publica la imagen al taggear (usado solo en CI)
GHCR_REGISTRY=ghcr.io
